.hover.show=h Shows.find_show_title(suggestion.show) if suggestion.show
.qstart “
.title_container
  .votes_container
    .votes=link_and_vote_count(suggestion, request.ip)
    .subtitle="vote#{suggestion.votes_counter == 1 ? '' : 's'}"
  .title=h suggestion.title
.qend ”
= haml :'suggestion/_timeago', :locals => {datetime: suggestion.created_at}
.arrow_user
  .user=h suggestion.user
  %span.arrow
